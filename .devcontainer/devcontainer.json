{
    "build": {
        "dockerfile": "Dockerfile"
    },
    "mounts": [
        "source=${localEnv:HOME}/.ssh,target=/home/developer/.ssh,type=bind,consistency=ro"
    ],
    "customizations": {
        "vscode": {
            "extensions": [
                "ziglang.vscode-zig"
            ]
        }
    }
}
